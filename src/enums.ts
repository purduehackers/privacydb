export enum Mode {
  OptIn = "opt_in",
  OptOutPrivacy = "opt_out_privacy",
  OptOutCollection = "opt_out_collection",
}

export enum Project {
  CommitOverflow = "commit-overflow",
  Ships = "ships",
}
